# Used by `pnpm dev` only. For production (Vercel), set NEXT_PUBLIC_API_URL
# in your Vercel project's Environment Variables instead — this file isn't
# deployed there.
NEXT_PUBLIC_API_URL=http://localhost:5000
